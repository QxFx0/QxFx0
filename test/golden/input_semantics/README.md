# Input Semantics Golden Fixtures

- `russian_school_parser_corpus_400.csv`: research corpus used for parser-layer regression authoring.
- `corpus_mapping.json`: canonical mapping from research route labels to runtime proposition/family types.

These fixtures are intentionally parser-facing and do not directly encode final `CanonicalMoveFamily` labels in every row.
