# QxFx0 progress tracker

Lightweight session-to-session log. Update only when there is real
news; do not turn this into changelog noise.

## Test-suite landscape (snapshot)

Last verified: 2026-05-18.

| Suite                      | Status | Note                                |
|----------------------------|--------|-------------------------------------|
| `qxfx0-test-integration`   | PASS   | 32 / 32                             |
| `qxfx0-test-slow`          | PASS   | 98 / 98                             |
| `qxfx0-test-property`      | PASS   | 80 / 80                             |
| `qxfx0-test-fast`          | PASS   | 445 / 445                           |
| `qxfx0-test-unit`          | PASS   | 451 / 451                           |
| `qxfx0-test`  (meta)       | PASS   | 567 / 567 — Phase 7 structural calibration infrastructure complete |

Repro: `cabal test all`.

All 8 historical failures resolved; see git log for fix commits.

## Regression locks (Package A consolidation)

Narrowly-scoped tests that will break if the corresponding fix is
regressed. Added 2026-05-18.

| Lock | File | What it guards |
|------|------|--------------|
| F1 | `Test.Suite.CoreBehavior` | `routeFamily` with holistic-leaning Field keeps `CMDeepen`; empty Field preserves cascade family (no salience escalation).  Retargeted to observable contract `rdFamily == delibReconciled.planFamily`. |
| F2 | `Test.Suite.TurnPipelineProtocol` | `tiConatusGateFired = True` drives `RecoveryConatusGate`; same energy without flag does NOT produce that cause (M6 invariant). |
| F3 | `Test.Suite.SelfAdjunction` | `arbitraryField` generator never crashes and always produces well-formed components (no `error` in generator path). |

## Recently landed (working tree, uncommitted)

- Package A (consolidation): three regression locks (F1/F2/F3),
  doc sync, dead-API Haddock markers, pending-work moved to
  ROADMAP.md.
- M2d (Phase 2.5): Conatus into Recovery as primary driver.
- Phase 5.5d: full pre-turn `Field` population (Resonance,
  Atmosphere, Consolidation, Counterfactual, Confidence).
- Phase 5.5e: trace observability for salience verdict.
- Phase 6 + M6.1: single-source-of-truth `tiConatusEnergy` /
  `tiConatusGateFired` / `tiField` plumbed through `PrepareStatic`
  → `TurnInput`.
- Audit follow-up: docstring sync (`Salience.hs:301`,
  `Salience.hs:341`, `Effects.hs:213`); deduplicated
  `isHolisticFamily` / `isFormalFamily` into `Self.Salience`.
- Audit follow-up (previous session): centralised modulation
  thresholds into `SalienceModulation` /
  `defaultSalienceModulation` in `Self.Salience`.
- Phase 8 Package B seed: pure `QxFx0.Self.Deliberation`
  reconciliation layer plus property locks for conatus override,
  agreement/idempotence, recovery preservation, tied fallback,
  divergence bounds, determinism, and trace tag totality.
- Phase 8 Package B (runtime integration): M1/M2/M3 wired
  (`routeFamily` → `reconcile` → `rdDeliberation` → `TurnPlan` →
  `buildLocalRecoveryPlan` override → `buildTurnProjection` trace
  population).  Four integration tests landed:
  - `testRouteFamilyDeliberationPopulated` (trace field population,
    `RuleAgreement`, zero divergence)
  - `testRouteFamilyConatusOverrideDeliberation` (`StyleRecovery` +
    `RecoveryConatusGate` when gate fires)
  - `testRouteFamilyAgreementIdempotence` (determinism / identical
    inputs yield identical `Deliberation`)
  - `testDeliberationRecoveryNotSilenced` (M3: deliberation cause
    survives even when local recovery planner returns `Nothing`).
- ADR-0011 accepted and written (`docs/adr/0011-deliberation-framework.md`).
  Updated with §9 Package C honest limits (observability-only divergence,
  inverted adjunction caller mapping noted for Package D fix, tone stub
  status) and §11 Package C completion addendum.
- Phase 8 Package C (observability-grade divergence): the holistic
  proposal now derives `planNarrativeTone` from field atmosphere
  (`NarrativeWarm` when arousal > floor and valence ≥ neutral,
  `NarrativeTerse` when arousal > floor and valence < neutral,
  `NarrativeNeutral` otherwise), while the formal proposal stays
  `NarrativeNeutral`.  Family and style remain identical
  (behaviour-preserving baseline), creating trace-visible
  `DivergeOnTone` under strong field signals without changing
  runtime output.
  New test `testRouteFamilyHolisticFieldDeliberationDivergence`
  asserts `DivergeOnTone`, `RuleSalienceLead`, reconciled tone
  `NarrativeWarm`, and exact 0.25 divergence.
- `DeliberationModulation` / `defaultDeliberationModulation` added
  to `Self.Deliberation` to centralise tone thresholds; previously
  hardcoded `0.5` / `0.0` in `routeFamily` now read from the record.
  This is the fifth modulation parameter family in the project
  (after legitimacy, parser, conatus, salience).
- `routeFamily` stale comment (Phase 8 M1/M2 era) rewritten to
  reflect Package C reality: tone-only divergence, observability-only.
- Regression locks F1/F2/F3 still active; no new locks required
  because Package C is tone-only divergence on an observability axis.
- Plan A (tech-debt cleanup, 2026-05-18): 8 S-effort fixes landed
  without runtime behaviour change:
  - A.1 `pickHigherSeverity` replaces Maybe-fallback for recovery
    cause merge in `Route/Render.hs` (Critical trap for Package D).
  - A.2 Dead import `renderStyleFromDecisionWithSalience` removed
    from `TurnRouting.hs`.
  - A.3 `turnInputSalience` helper deduplicates two inline copies
    of `salienceFromConatusEnergy` in `Finalize/State.hs`.
  - A.4 / A.5 Stale `tiField` / `psField` docstrings synced: all
    five components now populated, not just resonance.
  - A.6 `AGENTS.md` updated: landed phases 1-2/3/4/5/5.5d/5.5e/6/8
    explicitly listed; remaining work is Phase 7 + Package D.
  - A.7 `ROADMAP.md` updated: Near term #2 crossed out (landed),
    Long term #2 rewritten as Package D queue with accurate status
    of adjunction combinators (groundIn/probe production).
  - A.8 Verified: 553/553 PASS, zero new warnings.
- Phase 8 Package D (adjunction swap + family modulation cleanup):
  - `applySalienceEscalation` removed from `TurnRouting.hs`;
    `runFamilyCascade` (which already receives `routingSalience`) is
    now the single pre-deliberation family source.
  - `familyDivergenceEnabled = False` feature flag added;
    `nearestHolistic` retained as local helper for future B.3 work.
  - Adjunction caller mapping corrected (ADR-0008 orthodoxy):
    formal hemisphere probes the field, holistic hemisphere carries
    atmosphere-derived tone already baked into the Plan and is paired
    with `emptyField`.
  - Pre-mirror block (`preEgo`, `preIdentitySignal`, `preGuardReport`,
    `preSemanticInput`, `preSemanticAnchor`) inlined into `baseStyle`
    computation to remove redundant let-bindings.
  - F1 regression lock renamed from
    `testRouteFamilyEmptyFieldEscalatesToCMDescribe` to
    `testRouteFamilyEmptyFieldKeepsCascadeFamily`; retargeted to
    observable contract (`rdFamily == delibReconciled.planFamily`).
  - ADR-0011 §6.1 updated with new code structure, §9 inverted
    adjunction note marked resolved, §12 Package D addendum added.
- Verified: 553/553 PASS (meta), 431/431 (fast), 437/437 (unit),
  25/25 (integration), 74/74 (property), zero new warnings.

## Session 2026-05-18 — Phase 7 Step 2

- `FieldHeuristics` record with 8 tunable parameters + 3 pure compute
  functions (`computeConsolidation`, `computeCounterfactual`,
  `computeAtmosphere`) added to `QxFx0.Self.Field`.
- `buildPrepareEffectPlan` refactored to call the compute functions with
  `defaultFieldHeuristics`, removing ~35 lines of inline heuristic code.
- `Test.Suite.SelfField` expanded with 7 QuickCheck property tests:
  range gates for all three compute functions, monotonicity gates for
  topic-stability boost, streak boost, and legitimacy bonus.
- Verified: 438/438 fast, 444/444 unit, 560/560 meta (0 errors, 0 failures).
- ADR-0009 addendum (2026-05-18) records the constant mapping table.
- AGENTS.md updated: Phase 7 Step 2 completed; remaining work is
  `defaultSalienceWeights` calibration.

## Session 2026-05-18 — Phase 7 completion (structural calibration infrastructure)

- `Test.Suite.SelfSalience` expanded with 7 QuickCheck property tests
  for `computeSalience` / `defaultSalienceWeights`:
  - Range/priority gates: Conatus gate fires exactly when predicate
    says so; bias == 0.0 and confidence == 1.0 under gate.
  - Monotonicity: atmosphere arousal increase does not decrease bias.
  - Sanity: atmosphere valence does not affect salience (only arousal
    is read); field is completely ignored when Conatus gate fires.
  - Range: healthy Conatus confidence always in [0,1].
- Verified: 451/451 unit (7 new tests), 567/567 projected meta count.
- `ROADMAP.md` updated: Phase 7 crossed out as completed; runtime
  override of `FieldHeuristics` moved to long term (#7).
- `AGENTS.md` updated: Phase 7 marked completed; empirical tuning
  deferred to production trace corpora.

## Session 2026-05-19 — ADR-0012 proposed (Essence Commitment)

- `docs/adr/0012-essence-commitment.md` drafted as **Proposed
  (Phase 9, awaiting implementation)**.  Specifies the essence
  layer that sits atop the deliberation framework: who-am-I-in-
  essence as a forced Σ-typed commitment over an endogenous
  `EssenceTrajectory` of witnesses.
- Eight architectural forks resolved as the contract for Phase 9–10
  (revisability=irrevocable, granularity=coarse, witness=endogenous,
  coherence=required, extraction=deterministic, forcing=Conatus
  erosion + Angst threshold, augmentation-vs-constraint=constraint,
  cardinality=singular).
- Module shape proposed: `QxFx0.Self.Essence` with `Essence`,
  `EssenceTrajectory`, `EssenceWitness`, `EssenceCommitment`,
  `EssenceMode`, `CommitmentTrigger`, plus pure morphisms
  `witness` / `shouldCommit` / `extractMode` / `commit` and a
  `validatePlan` post-commitment guard.  Modulation centralised
  in `EssenceModulation` / `defaultEssenceModulation` (sixth
  modulation parameter family in the project).
- New exception variant planned for `QxFx0.ExceptionPolicy`:
  `EssenceRupture !Text` (categorical failure: the running system
  has acted contrary to what it has chosen to be).  Co-located
  with `IdentityRupture` in `Finalize.Commit`; orthogonal to it.
- Phase plan: Phase 9 ships infrastructure with the trajectory
  always `EssenceUncommitted` (no behavioural change); Phase 10
  flips `essenceCommitmentEnabled` and turns on `validatePlan`.
- Eight regression-lock candidates (E1–E8) defined for the new
  test suites `Test.Suite.SelfEssence` and
  `Test.Suite.SelfEssenceCommit`.
- Four open questions deferred to a Phase 9.3 review before the
  ADR moves from Proposed to Accepted (angst saturation
  semantics, trigger priority, `EssenceWitnessing` runtime
  visibility, validator location).
- No code changes this session; ADR-0012 is documentation-only.
  Test status unchanged from 2026-05-18 (567/567 PASS).
- Four ADR-0012 §11 open questions resolved (locked into the
  spec, not into the ADR yet — addendum lands as part of
  Step 9.7):
  - Q1 (saturation as trigger): NO — only threshold counts.
  - Q2 (trigger priority): Angst > Conatus on simultaneous fire.
  - Q3 (`EssenceWitnessing` visibility): YES — runtime-visible
    pre-commit mode (`trcEssenceMode = Just "witnessing"`).
  - Q4 (validator location): stays in `Finalize.Commit`;
    `reconcile` remains essence-unaware.
- `docs/phase-9-essence-implementation-spec.md` written as the
  complete TЗ for Phase 9 execution. Seven steps (9.1–9.7),
  +7 tests target (5 properties + 2 units), 574/574 PASS
  acceptance criterion, zero behavioural change. KIMI reads
  this spec + ADR-0012 and proceeds without clarifications.

## Session 2026-05-19 — Phase 9 Step 1 (infrastructure landed)

- `src/QxFx0/Self/Essence.hs` created: `Essence` Σ-type,
  `EssenceTrajectory`, `EssenceWitness`, `FieldSignature`,
  `EssenceMode`, `CommitmentTrigger`, `EssenceCommitment`,
  `EssenceModulation` / `defaultEssenceModulation`, pure morphisms
  `witness` / `shouldCommit` / `extractMode` / `commit`,
  `fieldSignature`, `renderEssenceMode`, `renderCommitmentTrigger`.
- `defaultDeliberation` fallback added to `QxFx0.Self.Deliberation`
  (exported) for turns that did not run `reconcile`.
- `ssEssence` threaded through `SystemState`; `tiEssence` through
  `TurnInput`; `psEssence` through `PrepareStatic`; populated in
  `buildPrepareEffectPlan` and plumbed through `buildTurnInput`.
- `buildNextSystemState` ingests one witness per turn via `witness`
  (using `defaultDeliberation` fallback); `ssEssence` always
  `EssenceUncommitted` in Phase 9.
- Four nullable trace fields added to `TurnReplayTrace`
  (`trcEssenceMode`, `trcEssenceCommitted`, `trcEssenceAngstLevel`,
  `trcEssenceTrigger`) and populated from post-turn essence in
  `buildTurnProjection`.
- `Test.Suite.SelfEssence` created with 7 tests (5 QuickCheck
  properties E1–E5 + 2 HUnit tests), wired into all six
  test-suite aggregators.
- Verified: 574/574 PASS (meta), 452/452 (fast), 458/458 (unit),
  74/74 (property), 91/91 (slow), 25/25 (integration),
  zero new warnings in modified modules.
- `AGENTS.md`, `ROADMAP.md`, `progress.txt` updated.
- ADR-0012 status flipped to **Accepted (Phase 9, infrastructure
  only)**; §11 open-questions addendum added per spec §0.
- `docs/phase-10-essence-commitment-implementation-spec.md`
  written. Ten steps (10.1-10.10), +8 tests target
  (`Test.Suite.SelfEssenceCommit`: E6 sticky, E7a/E7b always-
  admissible, sliding-window Q7, reconcile-courtesy C1, plus
  three flag-on/off integration locks), 582/582 PASS acceptance.
  Three Phase-10-specific decisions locked (Q5: pre-turn essence
  drives validation, not post-turn; Q6: validator computed in
  Finalize.State, thrown in Finalize.Commit; Q7: true sliding
  window over `etWitnesses` replaces Phase 9 approximation).
  Default `essenceCommitmentEnabled = False` until corpus replay
  confirms zero spurious ruptures.

## Session 2026-05-19 — Phase 10 Steps 10.1-10.9 landed

- KIMI executed Phase 10 Steps 10.1-10.9 in one session.
- `EssenceRupture !Text` added to `QxFx0.ExceptionPolicy`
  alongside `IdentityRupture` (categorical failure: system has
  acted contrary to what it has chosen to be).
- `EssenceViolation`, `validatePlan`, `admissibleFamilies`,
  `admissibleTones`, `admissibleStyles`, `renderEssenceViolation`
  added to `QxFx0.Self.Essence`. Always-admissible invariants
  (`CMRepair`, `NarrativeNeutral`) hold across every mode.
- `shouldCommit` rewritten with true sliding-window semantics
  over the last `emConatusFloorWindow` witnesses, replacing the
  Phase-9 approximation.
- `essenceCommitmentEnabled :: Bool` plumbed through
  `PrepareStatic` → `TurnInput` (M6 single-source-of-truth);
  default `False`, env var `QXFX0_ESSENCE_COMMITMENT_ENABLED`.
- Forced commit logic added to `buildNextSystemState`:
  under flag, `shouldCommit` → `commit` transitions
  `EssenceUncommitted` to `EssenceCommitted`.  Sticky:
  committed essences never revert; witnesses still ingested
  for diagnostic continuity.
- `computeEssenceValidation` in `Finalize.State` validates
  the reconciled `Plan` against `tiEssence` (pre-turn) when
  flag is on and pre-turn essence is committed (Q5: a turn
  that *causes* commitment is not validated by it).
- `EssenceRupture` thrown in `Finalize.Commit` immediately
  after `checkBlanketTransition`, before any persistence.
  Co-located with `IdentityRupture`; the two failures are
  orthogonal.
- Reconcile-time courtesy: `reconcile` accepts an optional
  `Maybe (Plan -> Bool)` predicate; `RuleTiedFallback` switches
  to `hPlan` only when `hPlan` admissible AND `fPlan` not.
  Never widens (locked by C1).
- `Test.Suite.SelfEssenceCommit` created with 8 tests:
  - `propStickyCommitment` (E6)
  - `propRepairAlwaysAdmissible` (E7a)
  - `propNeutralToneAlwaysAdmissible` (E7b)
  - `testFlagOffNoBehaviouralChange` (regression)
  - `testFlagOnCommitmentFires` (HUnit)
  - `testFlagOnRuptureThrows` (HUnit)
  - `propSlidingWindowExactSemantics` (Q7)
  - `propReconcileCourtesyNeverWidens` (C1)
- Verified: 582/582 PASS (meta), 460/460 (fast),
  466/466 (unit), 74/74 (property), 91/91 (slow),
  25/25 (integration), zero new warnings in modified modules.

## Session 2026-05-19 — Phase 10 Step 10.10 spec written, awaiting execution

- KIMI's claim that the corpus replay was "implicitly verified"
  by the default-off 582 PASS run is rejected.  With
  `essenceCommitmentEnabled = False`, `validatePlan` is never
  invoked, so zero ruptures is a tautology.  ADR-0012 §9 lock
  E8 (zero corpus ruptures under flag-on) has not been
  exercised.
- `docs/phase-10-step-10-corpus-replay-spec.md` written as a
  narrow follow-up ticket: run `qxfx0-test-integration` with
  `QXFX0_ESSENCE_COMMITMENT_ENABLED=1`, observe zero ruptures,
  then sync four doc artifacts (progress.txt, ROADMAP.md,
  AGENTS.md, ADR-0012 → Accepted (Phase 9 + 10, fully landed)).
- Hard rules in the spec: do not modify `validatePlan` to
  make tests pass; do not catch `EssenceRupture`; do not flip
  the default of `essenceCommitmentEnabled` in this ticket.
  Calibration deltas (if any rupture observed) live in
  `EssenceModulation` defaults, not in the validator.
- Two test-tightening follow-ups (`testFlagOffNoBehaviouralChange`
  and `propStickyCommitment` are tautological as currently
  written) noted explicitly out of scope for Step 10.10;
  tracked separately for a future ticket.

## Session 2026-05-19 — Phase 10 corpus replay (Step 10.10)

- `QXFX0_ESSENCE_COMMITMENT_ENABLED=1 cabal test qxfx0-test-integration`
  ran with: 25 / 25 PASS, zero `EssenceRupture` events observed in the
  test log.
- Observed commitment dynamics on the integration corpus:
  - Total turns across 25 sessions: not measured (integration test
    harness does not expose `replay_trace_json` to a queryable place).
  - Turns with `trcEssenceCommitted = true`: 0 (no matches in log).
  - Highest `trcEssenceAngstLevel` observed: not measured.
  - Commitment path is wired but not exercised by the current
    short-session integration corpus.  Longer-session corpus replay
    (manual or scripted, future work) will exercise the commit and
    rupture branches.
- ADR-0012 status flipped to **Accepted (Phase 10, fully landed)**.
- ROADMAP.md and AGENTS.md updated to reflect Phase 10 closure.
- Test status: 582 / 582 PASS unchanged from Step 10.9.

2026-05-19  Phase 10 completed (all steps, 582/582 PASS, zero ruptures on corpus replay)

## Session 2026-05-19 — Post-Phase-10 roadmap closure spec written

- `docs/post-phase-10-roadmap-closure-spec.md` written: six numbered
  sections covering all deferred items from the Phase 10 post-mortem.
  Each section classified as Execution / Empirical / Operations /
  Documentation / Design.
- Execution order (dependency-aware):
  - §1 test-tightening of two tautological essence tests (S)
  - §6.7 runtime override of `FieldHeuristics` (S-M)
  - §6.4 release artifact reproducibility audit (M)
  - §6.5 broader interop documentation (M)
  - §3 longer-session corpus harness (M-L)
  - §4 calibration addendum to ADR-0012 (M, depends on §3)
  - §2 production flag-flip checklist (S, doc only, depends on §4)
  - §6.6 effects-interpreter conatus-aware prior spec (L, spec only)
  - §5 four future-ADR triage stubs (M, triage only)
  - §6.3 domain reasoning packs triage stub (M, triage only)
- Cross-cutting acceptance: 588/588 PASS after §3 + §6.7;
  no full ADRs produced by KIMI (only triage stubs); flag-flip
  remains operator decision.
- Hard rules: one section per session, design-triage sections
  produce stubs not full ADRs, operations sections produce
  documentation not actual flips, test count never decreases.

## Session 2026-05-25 — Tech-plan closure regression locks landed

Five targeted regression tests added closing the gaps surfaced by the
post-Phase-10 audit on top of KIMI's previous focused-coverage wave.

Tests added:

- `Test.Suite.Guardrails.testCanSubmitOnTurnZeroWithEmptyState` —
  locks ADR-0030 §5.2 regression (`circuitBreakerOpen` treating
  `gsCooldownExpiry = 0` as an open circuit on turn 0).
- `Test.Suite.SemanticCorpus.testFallbackMatcherInventoryHasBreadth` —
  locks ADR-0030 §5.1 regression (`detectKeywordFallbackType`
  accidentally truncated to 3 of 31 matchers).  Eight single-token
  inputs across distinct matcher categories; assertion fails
  decisively if the chain regresses to the truncated state.
- `Test.Suite.KnowledgeTree.testPruneFruitsCleansUnvalidatedQuarantineUnderBulkInsertion` —
  partial mitigation for audit-round3 P0 #3 (`ktQuarantine` unbounded).
  Confirms `pruneFruits` drains unvalidated entries even under bulk
  insertion of 500 fruits.  Validated-but-rejected accumulation
  remains an open residual gap requiring a hard cap.
- `Test.Suite.KnowledgeTree.testRootStressSignalIsNaNSafe` — locks
  regression G1 for the calibration-signal boundary.
- `Test.Suite.SelfPerspective.testPerspectiveCandidateIsNaNSafe` —
  locks regression G1 for the perspective-candidate boundary
  (confidence, counterargument pressure, normative alignment,
  internal tension all finite + clamped to unit interval).

Source fixes (minimal, surgical) to make the NaN tests pass:

- `src/QxFx0/Self/Perspective.hs:761-764` — `clampUnit` now collapses
  NaN to 0.0 (fail-closed lower bound) before the existing
  [0.0, 1.0] clamp.
- `src/QxFx0/Learning/KnowledgeTree.hs:358-364` — same `clampUnit`
  NaN-safety hardening at the calibration-signal boundary.

Export expansion:

- `src/QxFx0/Semantic/Proposition.hs` — exports
  `detectKeywordFallbackType` so the matcher-inventory regression
  lock can invoke it directly without going through full
  `parseProposition`.

Validation:

- `cabal build lib:qxfx0 -j1` — green (only pre-existing warnings).
- `cabal build test:qxfx0-test-unit test:qxfx0-test-fast test:qxfx0-test -j1` — green.
- `cabal test qxfx0-test-unit -j1` — 701 / 701 PASS (was 696, +5).
- `cabal test qxfx0-test-fast -j1` — 730 / 730 PASS (was 725, +5).

Residual gaps explicitly NOT closed in this session (deferred to the
larger remediation plan):

- audit-round3 P0 #3 hard cap on validated quarantine entries.
- G1 cross-layer NaN property test (`prop_NoNaNInSelfLayer`,
  `prop_NoNaNInLearningLayer`).
- Architectural invariants test (Self.* layer acyclicity).
- DialogueDevelopment / PerspectiveRegistry focused suites.

2026-05-25 — ADR-0032 DialogueDevelopment focused suite closure

Implemented and registered `Test.Suite.DialogueDevelopment` as the focused
unit lock for the live-but-conservative dialogue-development contour.

Coverage landed:

- outcome learning keeps `dolRecentOutcomes` bounded by
  `maxRecentOutcomeSamples`.
- outcome counters increment separately for weak acknowledgements and repair
  requests.
- strong update gates speech-policy mutation while weak/observational outcomes
  keep policy unchanged.
- speech pattern map remains bounded by `maxSpeechPatternEntries`.
- render-style adjustment preserves `StyleRecovery` and can choose
  `StyleDirect` only when the speech policy has enough directness evidence.
- weak acknowledgement detection recognizes the current cue list and rejects
  non-cues.
- `normalizeDialogueText` locks the current replace-before-lowercase contract.
- `DialogueOutcomeLearningState`, `SpeechPolicyState`, and `BeliefStore`
  survive JSON round-trips.

Registration:

- `test/TestMainUnit.hs` imports and concatenates `dialogueDevelopmentTests`.
- `test/TestMainFast.hs` imports and concatenates `dialogueDevelopmentTests`.
- `test/TestMain.hs` imports and concatenates `dialogueDevelopmentTests`.
- `qxfx0.cabal` lists `Test.Suite.DialogueDevelopment` in the shared
  `test-common` `other-modules` stanza.

Validation:

- `cabal test qxfx0-test-unit -j1 --test-show-details=direct` — 711 / 711 PASS.
- `cabal test qxfx0-test-fast -j1 --test-show-details=direct` — 740 / 740 PASS.
- `cabal build test:qxfx0-test -j1` — compiles after registering the aggregate
  import.
- `cabal test qxfx0-test -j1 --test-show-details=direct` — aggregate executes
  but currently reports unrelated pre-existing/runtime-environment failures in
  `RuntimeInfrastructure` and `HttpRuntime` (5 errors, 7 failures out of 879);
  no failure is from `DialogueDevelopment`.

Next focused suite:

- `PerspectiveRegistry`, per P4 canonical versioned lineage source contract.

2026-05-25 — P4 PerspectiveRegistry focused suite closure

Implemented and registered `Test.Suite.PerspectiveRegistry` as the focused
unit lock for the canonical versioned lineage source behind P4
`OpinionCore / PerspectiveOperator`.

Coverage landed:

- observe-only decisions update the evaluated turn without allocating a
  perspective id, version id, lineage thread, or projection.
- first promotion creates a canonical active thread, active endorsed version,
  revision entry, and safe projection.
- active revision promotes a new version, marks the prior active version
  `PerspectiveRevised`, and keeps the newest revision first.
- per-scope revision history respects `prMaxRevisionsPerScope`.
- inactive endorsed versions respect `prMaxInactiveVersions`.
- global active cap suspends older active scopes without deleting lineage
  threads.
- explicit suspension clears active projections.
- registry JSON round-trip preserves lineage.
- duplicate normative profile ids fail decode rather than silently overwriting
  persisted profile state.
- public `PerspectiveProjection` exposes only capped summary/count/provenance
  surfaces while canonical registry entries keep bounded support and
  counterargument lists.

Registration:

- `test/TestMainUnit.hs` imports and concatenates `perspectiveRegistryTests`.
- `test/TestMainFast.hs` imports and concatenates `perspectiveRegistryTests`.
- `test/TestMain.hs` imports and concatenates `perspectiveRegistryTests`.
- `qxfx0.cabal` lists `Test.Suite.PerspectiveRegistry` in the shared
  `test-common` `other-modules` stanza.

Validation:

- `cabal build test:qxfx0-test-unit -j1` — green.
- `cabal test qxfx0-test-unit -j1 --test-show-details=direct` — 721 / 721 PASS.
- `cabal test qxfx0-test-fast -j1 --test-show-details=direct` — 750 / 750 PASS.
- `cabal build test:qxfx0-test -j1` — green.
- `cabal test qxfx0-test -j1 --test-show-details=direct` — aggregate executes
  but still reports unrelated runtime-environment/infrastructure failures in
  `RuntimeInfrastructure` and `HttpRuntime` (5 errors, 7 failures out of 889
  on the diagnostic rerun); no failure block is from `PerspectiveRegistry`.
