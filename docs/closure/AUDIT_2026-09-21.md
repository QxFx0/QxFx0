# Skeptical audit 2026-09-21 — real status

Method: read-only, no builds/runs. Every number recounted; every claim
sample-checked against code. Subagents unavailable (provider tier);
single-threaded by the operator.

## Data integrity: FIXED during audit

1. **40 v1 labels unattributed**: report claimed `rater: human-v1`,
   corpus rows had no rater stamp. Stamped + verified.
2. **5 disputed unmarked**: `adjudicated/disputed-v1.json` listed ids
   but corpus rows lacked `labels.disputed`. Stamped. (De-facto
   harmless: all 5 are uncovered-stratum, never entered the covered
   ranking evals — verified in `structscore_eval.json`.)
3. After stamping, `train_eligible = 125` recomputes exactly
   (35 + 90). Manifest composition note confirmed accurate.

## Code findings

4. **Dead field (minor)**: `repExternalActionDecision`
   (`Route/Render.hs:192`) is written (line 556) but never read
   anywhere in src/ or test/. Its Trace sibling is consumed. Remove
   or wire up.
5. **Rescue wiring verified**: both non-crisis/anomaly render branches
   append `rescueSuffix`; guard/finalize/rebind operate downstream on
   the suffixed text — no bypass. One honest edge: if the safety
   guard blocks the turn, the trace still records `rescue=` (tag
   describes detection, not delivery).
6. **No Debug.Trace, no TODO/FIXME/XXX/HACK in src/** (grep-clean).
7. **Test registration clean**: 138 `Test.Suite.*` modules, all
   registered in some TestMain, none missing.

## Test interpretation warnings

8. **4 true silent skips** (not 18 — first count was syntactic;
   per-site reading shows the rest are correct negative-assertion,
   cleanup, or documented-acceptance branches): 3× gf-gated in
   CoreBehavior + 1× qxfx0-main-gated Agda witness in
   RuntimeInfrastructure. All four now emit `SKIP(test): …` instead
   of passing silently. On this box gf exists (those run); the Agda
   site genuinely skips (binary not on PATH) — now visibly.
9. QXFX0_SLOW_GROUP / QXFX0_*_TEST env filters allow running
   fractions of slow — per-group discipline is documented, not a
   hole, but "slow green" must always state the grouping.

## Docs fixed during audit

10. AGENTS.md `moveDriftMargin` 0.10 → 0.20 (v4, code + THRESHOLDS
    already had it).
11. THRESHOLDS.md stale blocker (corpus absence) → condition met
    2026-09-21; blocker is now the objective, not the data.
12. CALIBRATION_CORPUS.md: strata table annotated (1000 → 1030),
    prelabel vs assistant-pre terminology disambiguated.

## Standing open (not closable here)

- Dead field removal (item 4) — needs owner decision.
- push/B2/brain_kb-externals — see session log 2026-09-21
  (substrate restored + verified same day).
- Verb-focus extractor debt (bestTopic elects verbs) — filed in
  CALIBRATION_REPORT.md EmptyHold section.

## Addendum 2026-09-22 — audit plan executed

- Dead field removed (`repExternalActionDecision` was write-only).
- Skips made visible (4 true tool-gates; the "18" was corrected).
- `qxfx0-test` runs only via `QXFX0_AGGREGATE_GROUP`
  (core 1190 + runtime 94 + http 23).
- Verb-focus: `trcBestTopic` trace observability landed (JSON
  backward-compat); live proof `связано` vs focus `добро`; probe
  rule pre-registered in CALIBRATION_REPORT.md, no behavior change.
