# QxFx0 Resource Contract Matrix

| resource_name | owner | where_used | probe_stage | strict_mode_behavior | degraded_mode_behavior | health_field | readiness_field | startup_consequence | policy_class |
|---|---|---|---|---|---|---|---|---|---|
| SQLite DB path / schema | runtime | bootstrap, turn execution, readiness | readiness, bootstrap, runtime | fail closed | fail closed | `db_alive`, `schema_ok`, `schema_reason` | `RcDatabase`, `RcSchema` | bootstrap blocked when inaccessible/inconsistent | required_fail_closed |
| Morphology core JSONs (`prepositional`, `genitive`, `nominative`) | resources | parser/morphology/runtime render | readiness, bootstrap | fail closed | fail closed | `morpho_ok`, `morph_backend` | `RcMorphology` | bootstrap blocked on missing/invalid JSON | required_fail_closed |
| `forms_by_surface.json` | resources | runtime morphology enrichment | readiness, bootstrap | fail closed | fail closed | `morpho_ok` | `RcMorphology` | bootstrap blocked on missing/unparseable file | required_fail_closed |
| GF map (`lexicon_funmap.tsv`) | lexicon | render / GF lexeme resolution | readiness, runtime | fail closed | degraded/not-ready for authoritative GF contour; render must fall back explicitly with `gf_map_unavailable:*` reason, and generic-topic substitution must carry explicit `gf_default_lexeme*` derivation tags instead of silently passing as authoritative GF output | `gfmap_ok`, `gfmap_status`, `gfmap_entries`, `gfmap_issue` | `RcGfMap` | health/runtime-ready blocked when unavailable; GF-authoritative render path disabled | required_fail_closed |
| Sidecar script (`http_runtime.py`) | http perimeter | `qxfx0-main --serve-http` | on_demand | fail closed | fail closed | `/sidecar-health` startup event + protocol fields | n/a | `--serve-http` startup fails if script cannot be resolved from packaged locations | required_fail_closed |
| Agda witness / spec | formal layer | strict readiness / runtime verification | readiness, runtime | fail closed | degraded allowed | `agda_ok`, `agda_status`, `agda_issues` | `RcAgdaSpec` | strict bootstrap/runtime-ready blocked when unavailable; degraded contour reports optional degradation | optional_degraded |
| Nix policy (`concepts.nix`) + evaluator | policy layer | constitutional checks | readiness, runtime | fail closed | degraded allowed | `nix_policy_present`, `nix_ok`, `nix_issues` | `RcNixPolicy` | strict bootstrap/runtime-ready blocked when unavailable; degraded contour reports optional degradation | optional_degraded |
| Datalog rules / souffle runtime | reasoning bridge | shadow / structural checks | readiness, runtime | strict depends on runtime contour; rules presence checked | degraded allowed | `datalog_ok`, `datalog_issues` | `RcDatalogRules` | non-authoritative shadow degradation when unavailable | optional_degraded |
| Parser backend | semantic input | turn preparation | runtime | local rule-based only | local rule-based only | parser trace fields (`parser_backend`, `parser_status`, `parser_degradation_reason`) | n/a | no external resource dependency in production variant B | observational_only |
| PGF grammar binary | GF runtime | optional linearization path | on_demand | bounded degrade when unavailable | bounded degrade when unavailable | surfaced via turn/replay render diagnostics | n/a | render falls back away from GF-authoritative path, but only if GF map itself is authoritative | optional_degraded |
