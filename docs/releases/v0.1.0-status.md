# QxFx0 v0.1.0 Release Status

**Date:** 2026-06-27
**Status:** NOT READY — blocked by environment constraints and pre-existing build errors

## Completed This Session

### P4 Option A — Legacy structuredBody Gate Enforcement (LAST SECURITY GAP)
- **Status:** IMPLEMENTED ✅
- Added `validatePredicate` and `filterAdmissiblePredicates` to `GeneratedPredicateGate.hs`
  - PG1 (Specificity): spTopicForm non-empty
  - PG2 (Non-tautology): spRu ≠ topic word
  - PG3 (Non-emptiness): spRu and spEn both non-empty
- Added `selectPredicatesGated` wrapper in `Dialogue.hs` — filters predicates through gate, drops empty SelectedPredicate entries
- All 4 `selectPredicates` call sites replaced (lines 512, 523, 637, 659)
- ConfrontQ's `lookupChallengeResponse` also covered (receives predicates from gated `selectedPreds`)
- Fixed bare `head` violation in `gateSourceWhitelist` (architecture gate clean)
- `GeneratedPredicateGate.hs` compiles successfully (module 41/408, `-fno-code`)
- Full build blocked by pre-existing `System.hs` duplicate-exports error + 30s timeout

### C4 Dead Code Removal
- **Status:** COMPLETE ✅ (Phase 0 already did the work)
- Phase 0 removed 23 Python scripts (~13,495 LOC) + 1 Haskell module (Semantic.AuthorityParse)
- All 407 remaining Haskell modules verified alive (imported by src/test/app)
- 7 initially flagged 'dead' modules all have importers in test/ or app/
- Thresholds sub-modules re-exported via Constants.hs → 20+ importers
- slice-013 branch has 13K deletions but those are architectural changes, not dead code
- No more safe mechanical dead code removal possible

### B3 Mechanical Gates
- **Status:** IMPLEMENTED ✅ (was already coded, now properly wired)
- Test modules exist: B3MechanicalGateExecution.hs (220 lines), SemanticContentB3.hs (233 lines), SemanticRepairB3.hs (245 lines), ContentQualityGate.hs (125 lines)
- Guard module: Core/Guard/ContentQuality.hs (184 lines, 6 checks)
- Runtime/Gate.hs (95 lines)
- ContentQualityGate added to cabal test-common other-modules
- B3 tests wired into TestMainUnit and TestMain (were only in TestMainFast)
- Tests NOT verified passing — 30s build timeout prevents compilation

## Release Readiness Checklist (from docs/release_readiness.md)

### 1. Build/Test Gates — BLOCKED ❌
- `cabal build` fails on pre-existing `System.hs` duplicate-exports error
- 30s timeout prevents full compilation (LexemeEntries 84K lines)
- `ci_gate_contract.sh` cannot complete Gate 1 (build)
- **Requires:** Fix System.hs duplicate-exports + non-timeout-constrained environment

### 2. Runtime/Environment Gates — BLOCKED ❌
- Cannot run `cabal run qxfx0-main -- --runtime-ready` (build doesn't complete)
- **Requires:** Build to pass first

### 3. Dialogue Quality Gates — NOT STARTED ❌
- 500-prompt eval not run
- `scripts/run_release_dialogue_gate.sh` exists but requires running system
- **Requires:** Build + runtime to work

### 4. Routing Coverage Gates — NOT VERIFIED ❌
- Must-route cases defined but not tested in this environment
- **Requires:** Build to pass

### 5. Observability Gate — NOT VERIFIED ❌
- **Requires:** Running system

## Open Closure Items (from REMAINING_CLOSURE_CHECKLIST.md)

| Front | Status | Blocker |
|---|---|---|
| DATALOG-ROLE-001 | OPEN | Public role document needed |
| M7 production evidence | OPEN | Corpus/replay gate, authority round-trip |
| B2 human-eval | OPEN (design only) | Blocked on B3 gates mechanically passing |
| B3 mechanical gates | IMPLEMENTED | Tests need verification in non-timeout env |
| P4 Option A | IMPLEMENTED | Build verification needed |

## Architecture Violations (Pre-existing)

7 violations in `check_architecture.sh` (all pre-existing, not from our changes):
1. System.hs imports forbidden layer from Types
2. TurnProjection.hs imports forbidden layer from Types
3. Network.hs imports Bridge/Core/Runtime from Semantic
4. Inflection.hs bare partial functions (T.init — false positive)
5. Content.hs bare partial functions (head on line 655)
6. AtomStore.hs bare partial functions (atomHead field name — false positive)
7. PathFinder.hs bare partial functions (head on lines 244, 328)

## Path to v0.1.0 Release

1. **Fix System.hs duplicate-exports** — pre-existing build blocker
2. **Full build in non-timeout environment** — verify P4, B3, all modules compile
3. **Run `cabal test qxfx0-test-fast`** — verify B3 gates pass
4. **Run `ci_gate_contract.sh` core profile** — verify all gates pass
5. **Run 500-prompt dialogue eval** — verify quality metrics
6. **Close DATALOG-ROLE-001** — public role document
7. **Tag v0.1.0** — with known limitations

## Known Limitations for v0.1.0

- M6-FELT remains NOT PROVEN (B2 human-eval not run)
- 7 pre-existing architecture violations
- SystemState god-record (74+ fields)
- 48 silent swallow sites not addressed
- Proposition*Admission type consolidation in progress (pre-existing uncommitted changes)
